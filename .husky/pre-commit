branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ]; then
  echo "❌ Direct commits to main are not allowed. Create a feature branch first."
  exit 1
fi

bash scripts/check-server-actions.sh || exit 1

npx tsc --noEmit --incremental || exit 1

npx lint-staged
